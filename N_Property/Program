using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Program : MonoBehaviour
{
    Property mySalary = new Property();

    void Start()
    {
        // mySalary.SalaryP = 3; // set이 private이기 때문에 수정이 불가능하다.

        print(mySalary.SalaryP); // get은 public이기 때문에 호출이 가능하다.
    }
}
